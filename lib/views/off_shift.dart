import 'package:flutter/material.dart';

class OffShift extends StatelessWidget {
  const OffShift({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('You are Off Shift'),
      ),
    );
  }
}