
name: fare_rate_mm
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0
  my_keyboard:
    path: ./lib/packeges/my_keyboard
  ui:
    path: ./lib/packeges/ui
  provider: ^6.0.1
  firebase_core: ^1.7.0
  firebase_auth: ^3.1.2
  pinput: ^1.2.0
  cloud_firestore: ^2.5.3
  intl: ^0.17.0
  connectivity: ^3.0.6
  connectivity_plus: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
